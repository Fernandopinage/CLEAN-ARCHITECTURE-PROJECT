version: "3"

services:

  db:
    image: mariadb
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: root
      MYSQL_DATABASE: LFPC
      MYSQL_USER: root
      MYSQL_PASSWORD: root
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
